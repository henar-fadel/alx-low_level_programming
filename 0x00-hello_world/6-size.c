#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        int c = (a);
        int t = (b);
        int lt = (c);
        int llt = (d);
        int f = (e);
        ptintf("Size of a char: %d byte(s), sizeof (a));
        ptintf("Size of a int: %d byte(s), sizeof (b));
        ptintf("Size of a long int: %d byte(s), sizeof (c));
        ptintf("Size of a long long int: %d byte(s), sizeof (d));
        ptintf("Size of a float: %d byte(s), sizeof (e));
    return (0);
}

